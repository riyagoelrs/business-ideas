{
  "name": "deckcleaner",
  "version": "0.2.0",
  "private": true,
  "description": "DeckCleaner deck QA editor with a dependency-light upload/scan/fix backend.",
  "scripts": {
    "start": "node backend/server.js",
    "check": "node --check backend/server.js && node --check backend/analyzer.js && node --check app.js && python3 -m py_compile backend/pptx_extract.py backend/fix_pptx.py"
  },
  "engines": {
    "node": ">=18"
  }
}
